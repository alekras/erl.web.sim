{application,
	sim_web,
	[
		{description, "This is a RESTful API of SIM server. The API manages SIM user&#39;s records. See for details https://github.com/alekras/erl.web.sim"},
	{vsn, "2.0.1"},
	{registered, []},
	{applications, [
		kernel, 
		stdlib, 
		public_key, 
		ssl, 
		lager,
		sasl,
		mqtt_client,
		mqtt_common,
		websocket_client,
		inets, 
		ranch, 
		cowboy, 
		jesse
	]},
	{mod, {sim_web, []}},
	{env, []},
	{modules, []},
	{licenses, ["Apache 2.0"]},
	{links, []}
]}.
